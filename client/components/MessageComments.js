import React from 'react';

const MessageComments = React.createClass({
    render() {
        return (
            <div>
                Comment!
            </div>
        )
    }
});

export default MessageComments;